'use client'
import {useState} from 'react'
import {AppShell} from '@/components/AppShell'
import {Send,CheckCircle2} from 'lucide-react'
export default function Profesor(){const [text,setText]=useState('');const [messages,setMessages]=useState(['Hola, Elba. Revisé 3.º básico y detecté que el OA 4 necesita reforzamiento en justificación de inferencias.']);const send=()=>{if(!text.trim())return;setMessages([...messages,text,'Propuesta preparada: guía de 3 páginas, juego inmersivo, ticket de salida y dos versiones adaptadas. Requiere tu aprobación antes de guardarse.']);setText('')};return <AppShell active="Profesor Virtual"><div className="page-head"><div><h1>Profesor Virtual YOYO</h1><p>Guía contextual con acciones reales y aprobación docente.</p></div><span className="tag">Contexto: 3.º básico · Lenguaje</span></div><div className="assistant-layout"><section className="chat-panel"><div className="chat-messages">{messages.map((m,i)=><div className={i%2?'chat user-msg':'chat ai-msg'} key={i}>{m}</div>)}</div><div className="chat-input"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Pide una planificación, adaptación, análisis o recurso..."/><button className="btn btn-primary" onClick={send}><Send size={16}/>Enviar</button></div></section><aside className="panel"><h2>Acciones preparadas</h2>{['Analizar OA y resultados','Crear guía principal','Generar versiones adaptadas','Preparar ticket de salida','Vincular al curso'].map((x,i)=><div className="action-row" key={x}><CheckCircle2 size={17}/><span>{x}</span><small>{i<2?'Listo':'Pendiente'}</small></div>)}<button className="btn btn-coral" style={{width:'100%',marginTop:16}}>Revisar y aprobar</button><div className="insight"><b>Control docente</b><p>YOYO no publica, asigna ni modifica información sensible sin autorización.</p></div></aside></div></AppShell>}
