'use client'
import {useState} from 'react'
import {AppShell} from '@/components/AppShell'
import {Volume2,Sparkles,Accessibility,MousePointer2,Play,Pause} from 'lucide-react'
export default function Juegos(){const [running,setRunning]=useState(false);const [found,setFound]=useState<string[]>([]);const toggle=(x:string)=>setFound(v=>v.includes(x)?v.filter(i=>i!==x):[...v,x]);return <AppShell active="Juegos"><div className="page-head"><div><h1>Juegos inmersivos</h1><p>Ambientes, sonido, animaciones e interacción con evidencia pedagógica.</p></div><button className="btn btn-coral" onClick={()=>setRunning(!running)}>{running?<Pause size={17}/>:<Play size={17}/>} {running?'Pausar':'Iniciar escena'}</button></div><div className="tool-row"><span className="control-chip"><Volume2 size={16}/>Sonido activo</span><span className="control-chip"><Sparkles size={16}/>Animaciones</span><span className="control-chip"><Accessibility size={16}/>Modo accesible</span><span className="control-chip"><MousePointer2 size={16}/>4 objetos interactivos</span></div><div className="game-layout"><section className="immersive-stage"><div className="sky"><div className="cloud c1"/><div className="cloud c2"/></div><div className="forest-ground"><div className="tree t1">🌳</div><div className="tree t2">🌲</div><div className="character">👧<small>Sofía</small></div>{[['🎒','Mochila'],['📝','Nota'],['🐦','Ave'],['🏠','Cabaña']].map(([icon,label],i)=><button key={label} className={'hotspot h'+(i+1)+(found.includes(label)?' found':'')} onClick={()=>toggle(label)}><span>{icon}</span><b>{label}</b></button>)}</div><div className="mission"><b>Misión:</b> encuentra dos pistas que expliquen por qué Sofía no quiere entrar todavía a la cabaña.<div className="mission-progress">Pistas encontradas: <strong>{found.length}/2</strong></div></div></section><aside className="panel"><h2>Panel docente en vivo</h2><div className="metric-grid"><div><strong>18</strong><span>participando</span></div><div><strong>{found.length}</strong><span>pistas activas</span></div><div><strong>5</strong><span>usan narración</span></div><div><strong>3</strong><span>requieren apoyo</span></div></div><div className="insight"><b>Sugerencia YOYO</b><p>Modela cómo una pista del ambiente se conecta con una inferencia.</p></div></aside></div></AppShell>}
