name: Production Repair - Archived Safety Record

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: One-time repair completed
        shell: bash
        run: |
          echo "La reparación única de producción ya fue completada y verificada."
          echo "Este workflow está archivado y no modifica variables, despliegues, dominios ni aliases."
          echo "Los cambios futuros deben pasar por validación aislada y release explícito."
