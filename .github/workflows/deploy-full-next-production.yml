name: Deploy Full Next Platform Production - Manual Release

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  VERCEL_ORG_ID: team_Wq1pPeDlHDUfKwJNyH4ZSLFD
  VERCEL_PROJECT_ID: prj_90GNn7Y95ZZIYAJEyOoJCyi6NO0p
  VERCEL_SCOPE: elbarosalesc-pngs-projects

jobs:
  release-safety-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Production release policy
        shell: bash
        run: |
          echo "Este workflow no despliega automáticamente."
          echo "El release de producción sólo se ejecuta manualmente tras aprobar build, rutas, Supabase, YOYO IA y checklist funcional."
