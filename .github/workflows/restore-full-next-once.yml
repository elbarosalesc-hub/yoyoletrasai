name: Restore Full Next Platform - Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  safety-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Restoration safety policy
        shell: bash
        run: |
          echo "La restauración del dominio permanente es exclusivamente manual."
          echo "Antes de una restauración se exige validación completa de rutas, build, Supabase y YOYO IA."
