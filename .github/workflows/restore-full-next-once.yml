name: Restore Full Next - Archived Safety Record

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Restore workflow archived
        shell: bash
        run: |
          echo "La restauración única ya fue completada."
          echo "Este workflow no modifica dominios, aliases ni despliegues."
          echo "Toda publicación futura requiere release explícito después de validación aislada."
